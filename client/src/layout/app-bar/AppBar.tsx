import { Toolbar } from '@mui/material'

const AppBar = () => (
	<Toolbar
		sx={{
			// bgcolor: 'background.header',
			position: 'absolute',
			top: 0,
			left: 0,
			right: 0,
			height: 64,
		}}
	/>
)

export default AppBar
