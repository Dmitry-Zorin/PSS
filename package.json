{
	"name": "pss",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"start": "run-p start:*",
		"start:client": "npm start --workspace=client",
		"start:server": "npm start --workspace=server",
		"debug": "run-p start:client debug:server",
		"debug:server": "npm run debug --workspace=server",
		"vercel-build": "npm run vercel-build --workspace=server"
	},
	"devDependencies": {
		"@types/color": "^3.0.3",
		"npm-run-all": "^4.1.5"
	},
	"dependencies": {
		"color": "^4.2.3"
	}
}
